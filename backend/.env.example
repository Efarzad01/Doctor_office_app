# Server Configuration
NODE_ENV=development
PORT=5000

# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=doctor_office_db
DB_USER=postgres
DB_PASSWORD=your_password

# JWT Configuration
JWT_SECRET=your_jwt_secret_key_change_this_in_production
JWT_EXPIRE=7d

# CORS Configuration
FRONTEND_URL=http://localhost:3000

# Rate Limiting
RATE_LIMIT_WINDOW_MS=900000
RATE_LIMIT_MAX_REQUESTS=100

# Encryption Configuration (for sensitive patient data)
# IMPORTANT: Generate new keys for production using:
# node -e "const crypto = require('crypto'); console.log('ENCRYPTION_KEY=' + crypto.randomBytes(32).toString('hex'));"
# node -e "const crypto = require('crypto'); console.log('ENCRYPTION_IV=' + crypto.randomBytes(16).toString('hex'));"
ENCRYPTION_KEY=69c84f0ad3119286b6ebfced58f162a78493023239d2f633e8ce7a1671a8d42b
ENCRYPTION_IV=71228289ce065154bdc1b0994d79116a
